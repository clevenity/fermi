import <std.io>;
fn fact(n: int): int {
    if (n <= 1) { return 1; }
    return n * fact(n - 1);
}
fn fib(n: int): int {
    if (n <= 1) { return n; }
    return fib(n-1) + fib(n-2);
}
fn main(): int {
    io::print(fact(10));
    io::print(fib(20));
    return 0;
}
