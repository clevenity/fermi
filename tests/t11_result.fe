import <std.io>;
fn divide(a: int, b: int): Result<int, int> {
    if (b == 0) {
        return Err(1);
    }
    return Ok(a / b);
}

fn main(): int {
    let r: Result<int, int> = divide(10, 2);
    let r2: Result<int, int> = divide(7, 0);
    io::print("result tests ok");
    return 0;
}
