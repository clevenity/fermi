import <std.io>;
class Point {
    x: int;
    y: int;

    fn dist_sq(self): int {
        return self.x * self.x + self.y * self.y;
    }
}

fn main(): int {
    let p = Point { x: 3, y: 4 };
    io::print(p.dist_sq());
    io::print(p.x);
    return 0;
}
