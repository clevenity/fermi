import <std.io>;
import <std.mem>;

fn main(): int {
    region r {
        let p: *int = mem::alloc(64);
        let q: *int = mem::alloc(128);
        io::print("in region\n");
    }
    io::print("region auto-free ok\n");
    return 0;
}
