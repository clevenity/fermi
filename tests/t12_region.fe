import <std.io>;
import <std.mem>;
fn main(): int {
    region r {
        let p: *int = mem::alloc(16);
        let q: *int = mem::alloc(32);
    }
    io::print("region auto-free ok");
    return 0;
}
