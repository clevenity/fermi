import <std.io>;
fn main(): int {
    mut sum = 0;
    for (mut i = 1; i <= 10; i++) {
        sum += i;
    }
    io::print(sum);
    mut j = 0;
    while (j < 5) {
        j++;
    }
    io::print(j);
    do {
        j--;
    } => while (j > 0);
    io::print(j);
    return 0;
}
